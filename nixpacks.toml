[phases.setup]
nixPkgs = ["...", "python3"]

[phases.install]
cmds = [
  "apt-get update && apt-get install -y --no-install-recommends libglib2.0-0 libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf-2.0-0 libffi-dev shared-mime-info",
  "pip install -r requirements.txt"
]
