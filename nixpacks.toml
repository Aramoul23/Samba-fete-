[phases.setup]
nixPkgs = ["...", "cairo", "pango", "libffi", "gdk-pixbuf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
