{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn run:app --bind 0.0.0.0:${PORT} --workers 2 --timeout 120'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 90
  }
}
